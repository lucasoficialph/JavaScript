let despesas = {
    fardoArroz: 0,
    internet: 97.0,
    agua: 30.0,
    energia: 60.0,
    gas: 150.0
}

let total = despesas.value

/*let total = parseFloat(despesas.fardoArroz + despesas.internet + despesas.agua + despesas.energia + despesas.gas).toFixed(1)*/

console.log('Total de despesas: ' + total)